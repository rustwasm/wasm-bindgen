[package]
authors = ["The wasm-bindgen Developers"]
edition = "2021"
name = "sample"
publish = false
version = "0.0.0"

[lib]
test = false

[dependencies]
js-sys = { path = '../../js-sys' }
wasm-bindgen = { path = '../../..' }
wasm-bindgen-futures = { path = '../../futures' }

[dev-dependencies]
wasm-bindgen-test = { path = '..' }
