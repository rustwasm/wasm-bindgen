[package]
name = "sample"
version = "0.0.0"
authors = ["The wasm-bindgen Developers"]
edition = "2021"
publish = false

[lib]
test = false

[dependencies]
js-sys = { path = '../../js-sys' }
wasm-bindgen = { path = '../../..' }
wasm-bindgen-futures = { path = '../../futures' }

[dev-dependencies]
wasm-bindgen-test = { path = '..' }
